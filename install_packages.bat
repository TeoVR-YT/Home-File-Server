@echo off
npm -v && npm install express --save && npm install multer --save && npm install fs --save && npm install path --save && npm install nat-upnp --save && npm install readline --save && (
    cls
    echo All packages installed successfully!
    pause
)